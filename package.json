{
  "name": "hello2cc",
  "version": "0.0.7",
  "type": "module",
  "description": "Skill-free, native-first Claude Code plugin that now ships a default inherit-model main agent, mirrors active session models into native agents, and silently bootstraps native-first behavior for third-party models.",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/hellowind777/hello2cc.git"
  },
  "homepage": "https://github.com/hellowind777/hello2cc",
  "bugs": {
    "url": "https://github.com/hellowind777/hello2cc/issues"
  },
  "scripts": {
    "validate": "node ./scripts/validate-plugin.mjs .",
    "test": "node --test",
    "test:real": "node ./scripts/claude-real-regression.mjs",
    "check": "npm run validate && npm test"
  },
  "files": [
    ".claude-plugin/",
    "agents/",
    "hooks/",
    "scripts/",
    "output-styles/",
    "settings.json"
  ],
  "engines": {
    "node": ">=18"
  }
}
